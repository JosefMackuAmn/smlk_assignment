import supertest from 'supertest';

import { app } from '../../app';

describe('POST /auth', () => {
    it('sends 201 and creates new user', async () => {
        expect(true).toBe(true);
    });
});